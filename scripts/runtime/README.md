# scripts/runtime

生产运行时组件，勿归档、勿删除：这里的脚本由磁力链校准流程通过 `subprocess` 调用
（`bot/services/calibration_scripts.py` 与 `src/application/services/calibration_workflow_service.py`），
路径解析见 `src/core/utils/script_paths.py`。
