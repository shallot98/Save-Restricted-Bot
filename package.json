{
  "//": "前端已 vendored（static/js/*.min.js 为上游发行版），minify 流水线已移除：原 build/minify-*.js 未纳入版本库（.gitignore:19 'build/'），脚本在克隆后必然断链。",
  "name": "save-restricted-bot",
  "version": "1.0.0",
  "description": "Telegram Save Restricted Bot - Asset Optimization",
  "devDependencies": {
    "terser": "^5.36.0",
    "cssnano": "^7.0.6",
    "postcss": "^8.4.49"
  }
}
